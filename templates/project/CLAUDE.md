<!--
Шаблон CLAUDE.md проекта. Всё здесь грузится в КАЖДУЮ сессию: оплачивается
токенами каждый раз и конкурирует за внимание модели. Ориентир — 200 строк.

Сюда НЕ надо:
  - общие правила фреймворка (приедут модулем std-*, загрузятся по путям);
  - пересказ структуры директорий (модель прочитает сама);
  - пожелания без критерия проверки («пиши чистый код»).
-->

# Проект: ЗАМЕНИ

Одна строка: что делает система и для кого.

## Стек

- ЗАМЕНИ (например: PHP 8.3, Laravel 11, PostgreSQL 16, Vue 3, Redis, RabbitMQ)

## Команды

```bash
make test          # тесты
make lint          # стиль + статический анализ
make up            # поднять окружение
```

Полный прогон всех гейтов — `/std-gauntlet:run`.

## Карта слоёв

- ЗАМЕНИ (например: `app/Http/` — приём и отдача; `app/Actions/` — бизнес-операции)

Запрещённые направления зависимостей: ЗАМЕНИ.

## Здесь всегда так

<!-- 3–7 пунктов, только специфичное для этого репозитория -->

- ЗАМЕНИ
- Тесты не переписываем под реализацию: падает тест — чиним код.
- Перед коммитом гейты должны быть зелёными.

## Ловушки

<!-- То, на чём уже обжигались. Каждый пункт — с причиной. -->

- ЗАМЕНИ (например: модуль `legacy/Billing` не трогать без согласования
  с @finance — на нём завязана выгрузка в 1С, тестами не покрыт).

## Приоритет правил

Правила этого файла имеют приоритет над общими из `.claude/rules/std-*`.
Если видишь противоречие — следуй этому файлу и **скажи мне об этом**:
значит общее правило пора уточнять.

## Текущее состояние качества

<!-- Заполняется после первого прогона. Динамика важнее абсолютной цифры. -->

- покрытие: ЗАМЕНИ%
- mutation score: ЗАМЕНИ% (порог в `.claude/gauntlet.json`)

## Стандарты команды

Общие правила стека подключены модулями в `.claude/rules/std-*` (симлинки,
в git не хранятся). Если их нет — `/std-core:setup`.
Проверить, что загрузилось: `/context`, затем `/std-core:doctor`.
